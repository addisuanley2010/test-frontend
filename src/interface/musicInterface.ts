export interface MusicInterface {
  _id?: string,
  title: string;
  artist: string;
  album: string;
  gener: string;
}
export interface StatInterface {
  totalMusic: number,
    totalArtists: number,
   totalAlbums: number,
    totalGenres: number,
}